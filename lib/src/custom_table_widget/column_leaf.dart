class LeafColumn {
  final String headerTitle; // top-level header
  final String title; // leaf title (header or subHeader)
  final double width;

  LeafColumn(this.headerTitle, this.title, this.width);
}